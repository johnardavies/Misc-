#Example of a flask application that prompts the user for input writes it to an sqlite database and returns the input

# cd to the directory with bash directory starts mnt/c/
# setup a virtual environment in python venv
# Run the setupdatabase.py script i.e. python setupdatabase.py
# Run the flaskql script and the home page should be visible on http://localhost:5000/
